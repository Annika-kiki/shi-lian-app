# 食练周期上线清单

更新时间：2026-09-13

## 已完成

- [x] 确认正式 AppID：`wx26dfe00bf5f3258b`
- [x] 确认主体类型：个人
- [x] 确认个人主体名称：王嘉会
- [x] 确认首版范围：资料、体重、饮食、食谱、训练、统计
- [x] 确认首版不含支付、会员、广告、社交和在线大模型
- [x] 确认产品定位为普通健身饮食记录工具，不提供医疗诊断或效果承诺
- [x] 确认首版仅面向 14 周岁以上用户
- [x] 确认账户存续期间保存必要数据，注销后删除个人数据，备份到期清除
- [x] 恢复远程 Git 历史并创建本地上线准备分支
- [x] 后端测试、AI 演示测试、Python/JavaScript/JSON 语法检查通过
- [x] 完成首轮密钥、越权、隐私、包体和生产配置盘点
- [x] Python 依赖漏洞扫描通过，生产 npm 依赖扫描为零漏洞
- [x] 登录页要求主动勾选并可阅读用户协议和隐私说明
- [x] 过期会话自动安全恢复，并发请求共享单次登录
- [x] 保存失败不再伪装为本地保存成功
- [x] 登录和食谱生成接口具备应用层基础限流
- [x] 微信开发者工具服务端口已开启，本地项目编译并启动成功
- [x] 本地 `wx.login`、开发环境模拟登录回退及鉴权请求链路已联调通过
- [x] 会话绑定账户随机标识，注销或账户换代后旧令牌不可复用
- [x] “我的”、体重趋势和训练日历不再显示虚构的演示统计数据
- [x] 注销后同步清理服务端记录及本机饮食、食谱、资料和训练缓存
- [x] 模拟登录、自动建表和接口文档仅允许本机开发环境，staging 与 production 使用同等安全启动门禁
- [x] 用户明确勾选当前版本协议前不调用 `wx.login`，注销后要求重新同意
- [x] 修复资料页返回按钮误触发保存的问题
- [x] 修复食谱详情返回失效，并清理或接通无响应的图标按钮
- [x] 增加 WXML 事件绑定、隐私启动时序和固定演示数据自动门禁
- [x] 发布包排除未引用的旧组件副本和旧图片目录，源文件仍保留可恢复
- [x] 饮食、体重、训练和连续记录统一采用中国标准时间，避免 UTC 跨日错记
- [x] 食谱生成器去除4份被静默覆盖的重复实现，并补齐未知食材回归测试
- [x] 临时生成食谱不再永久写入公共食谱表，避免数据泄露面和无上限数据库增长
- [x] 用户主动填写的昵称已端到端保存；首版不申请微信头像昵称授权
- [x] 建立审核说明草案和逐项真机验收脚本
- [x] 对官方上传工具的遗留依赖风险增加隔离执行、受保护环境、最小权限、固定提交和密钥文件权限门禁
- [x] 拆分生产与测试依赖、固定已验证版本，并确认生产 Python 依赖无已知漏洞
- [x] 收紧生产配置白名单，阻止拼错环境名、空数据库地址、非 HTTPS 来源及通配符主机绕过启动门禁
- [x] 编写微信云托管端口、环境变量、零流量验证、迁移和回滚运行手册
- [x] 提供不落明文 SQL 的加密 MySQL 备份脚本和仅允许 staging 的校验恢复脚本
- [x] 建立提交审核证据矩阵，并在 CI 中增加容器镜像构建和健康检查冒烟测试
- [x] 微信云托管首个环境及 Flask 示例服务已初始化；该环境暂作为 staging 候选，不承载正式用户
- [x] 体验版接入 `wx.cloud.callContainer`，后端按 AppID、云环境 ID 和微信注入 OpenID 校验身份，不再要求创建 AppSecret
- [x] 增加部署数据库隔离门禁：staging/production 必须使用各自数据库，且服务拒绝以 root 启动

## Codex 可处理

- [x] 接入 `wx.login` 与服务端微信登录，建立不可伪造、可过期的会话
- [x] 在生产环境禁用模拟登录及任意 `X-User-Id` 鉴权
- [x] 增加伪造用户编号、篡改会话、会话过期和微信登录测试
- [x] 增加用户注销与个人数据删除能力
- [x] 增加隐私说明入口、14 周岁年龄限制和健康免责声明
- [x] 排除未引用素材和示例视频，生成轻量发布图片并增加包体门禁
- [x] 建立开发、测试和生产环境矩阵及生产配置启动门禁
- [x] 禁止体验版和正式版接口地址被本地缓存覆盖
- [x] 引入固定、可审计的数据库迁移，验证空库升级且模型无遗漏
- [x] 建立 CI、版本号、变更记录和受人工确认保护的 `miniprogram-ci` 配置
- [x] 编写部署、回滚、备份、故障处理和密钥管理文档
- [ ] 部署 FastAPI 后补齐实际服务版本、数据库内网连接、监控与回滚证据

## 需要负责人决定

- [x] 后端优先保留 FastAPI，同时允许评估低成本微信云开发方案
- [x] 确认采用微信云托管承载 FastAPI，并使用 CloudBase MySQL；实际套餐和超额计费仍须负责人确认
- [ ] 确认隐私政策最终文本（运营者王嘉会、公开联系方式 2797825558@qq.com 已确认）
- [x] 确认备份保存周期采用建议的30天
- [x] 确认备份方案：CloudBase 内置7天自动备份，并额外保留30天独立加密逻辑备份
- [ ] 开通前核对独立备份所需存储的实际价格并由负责人确认购买

## 需要负责人操作或确认

- [x] 负责人提供微信公众平台关键页面截图，供 Codex 只读核查
- [x] 开启微信开发者工具服务端口，供 Codex 执行本地编译检查
- [x] 截图确认个人主体、已通过服务类目及小程序备案状态
- [ ] 确认任何付费云资源、域名或证书采购
- [ ] 创建或下载微信/CI 密钥
- [ ] 真机验收体验版
- [ ] 确认远程推送、代码上传、提交审核和正式发布

## 当前阻塞

- [x] 微信后台截图已核验：个人主体、两个服务类目均已通过、小程序已备案、用户隐私保护指引已更新
- [ ] CloudBase MySQL 已由模板初始化，但尚未创建最小权限应用账号、执行本项目迁移或验证备份
- [x] 初始 MySQL root 密码曾出现在配置截图中；负责人已确认完成重置，仓库和后续截图不保存该密码
- [ ] 创建最小权限业务账号并从服务环境变量移除 root 凭据；业务版本不得使用 root
- [ ] CloudBase 个人版数据库缺少内置回档能力，正式上线前必须确定并验证独立恢复路径
- [ ] FastAPI 尚未替换 Flask 示例；替换后必须关闭服务公网访问并完成云身份真机联调
- [ ] 正式隐私政策尚待负责人最终接受；运营者、公开邮箱及后台隐私保护指引状态已核验

## 发布门禁

只有同时满足以下条件才可建议上传审核：

1. 所有 P0 安全问题关闭，并通过权限隔离测试。
2. 微信开发者工具编译、代码质量检查和真机回归通过。
3. 测试环境与正式环境隔离，生产环境有监控、备份和回滚方案。
4. 后台类目、备案、域名和隐私保护指引与实际功能一致。
5. 负责人明确确认本次版本、审核材料和上传动作。
