# 微信后台只读核验清单

## 2026-09-05 截图核验结果

- 小程序名称：食练周期。
- AppID：`wx26dfe00bf5f3258b`。
- 主体：个人，负责人确认完整名称为王嘉会。
- 小程序备案：已完成，备案号为 `浙ICP备2026066675号-1X`。
- 微信认证：未认证；后台注明未认证不影响后续版本发布，但会限制被搜索、被分享等能力。
- 服务类目：`体育 > 在线健身`、`工具 > 健康管理`，均已通过。
- 用户隐私保护指引：已更新。
- 用户生成内容场景：未声明；与首版不含社交和 UGC 的范围一致。
- 小程序订单中心 path：待设置；首版不含支付，无需在本阶段设置。
- AppSecret 与代码上传密钥：页面显示“生成”，未生成且未执行任何操作。
- 服务器域名：尚未配置；后台提示采用微信云开发或微信云托管可免配置服务器域名。
- 微信云托管：已创建环境 `prod-d4g1s6f9gaef2c320` 和 Flask 示例服务 `flask-3xwi`；业务后端尚未部署。

以上结论来自负责人提供的后台页面照片；提交审核前仍需在同一后台复核是否有状态变化。

由于当前自动化浏览器不能访问微信公众平台，请负责人登录 `mp.weixin.qq.com` 后按下列清单提供截图。截图前可遮挡身份证号、手机号、AppSecret、密钥和其他无关敏感信息。

## 需要截图

1. 设置与开发 → 基本设置：AppID、主体类型、小程序名称、认证和备案状态。
2. 设置与开发 → 服务器域名：request 合法域名；不要打开或截图 AppSecret。
3. 设置与开发 → 开发管理：当前开发者、体验成员和基础库配置，不显示密钥。
4. 服务类目：当前已选类目及审核状态。
5. 用户隐私保护指引：已声明的信息类型、用途和生效状态。
6. 版本管理：开发版、审核版、线上版状态；不要点击提交审核或发布。
7. 云开发/云托管入口：是否已有环境、免费额度、套餐和欠费状态；不要点击开通或购买。
8. 违规记录或站内通知：仅截图与审核、类目、备案相关的通知。

## 需要核对的一致性

- AppID 必须为 `wx26dfe00bf5f3258b`。
- 首版功能必须与服务类目允许范围一致。
- 隐私指引覆盖账号标识、用户主动填写的昵称、年龄、性别、身高、体重、饮食和训练记录；首版不声明微信头像昵称授权。
- 首版不应声明或申请手机号、定位、通讯录、相机、相册、麦克风、支付、广告或在线 AI 权限。
- request 域名必须与 `frontend/config/env.js` 的正式 HTTPS 地址完全一致。

## 禁止操作

只读核验期间不要重置 AppSecret、生成上传密钥、开通付费资源、接受新协议、修改主体/类目、上传代码、提交审核或发布版本。
