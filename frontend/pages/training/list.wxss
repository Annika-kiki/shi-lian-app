@import "../../styles/common.wxss";

.search-box {
  width: 100%;
  height: 96rpx;
  border-radius: 26rpx;
  border: 2rpx solid var(--line);
  padding: 0 28rpx;
  box-sizing: border-box;
  background: var(--surface);
  box-shadow: inset 0 0 0 2rpx rgba(56, 142, 60, 0.025);
  font-size: 30rpx;
}

.exercise-row {
  display: grid;
  grid-template-columns: 140rpx minmax(0, 1fr);
  gap: 22rpx;
  align-items: center;
}

.exercise-thumb {
  width: 140rpx;
  height: 140rpx;
  border-radius: 22rpx;
  background: var(--mint);
  display: block;
}

.exercise-info {
  min-width: 0;
}
